// Create a Vue instance
const app = new Vue({
    el: '#app',
    data: {
        link: 'https://www.openai.com',
        blabla:"hello guys" // Initialize the link data property
    },
});

// You can also dynamically update the link property later if needed
// app.link = 'https://www.google.com';
